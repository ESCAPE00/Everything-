using UnityEngine;

public class TripleShoot : Gun
{
    //public override void Shoot()
    //{
    //    if (CurrentReloadTime <= 0)
    //    {
    //        for (int i = -1; i < 2; i++)
    //        {
    //            Bullet bullet = Instantiate(BulletPrefab, BulletSpawnPoint.position + Vector3.right * i, Quaternion.identity);
    //        }
    //    }

    //    CurrentReloadTime = ReloadTime;
    //}
}
